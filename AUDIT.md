# AUDIT.md

Append-only activity log. One timestamped entry per completed feature or fix, newest last. Format:

```
## YYYY-MM-DD HH:MM — <feature or fix name>
<what was done, in a sentence or two>
```

---

## 2026-08-23 20:17 — Feature 00 · git-ai + repo skeleton

Completed feature 00. Added `go.mod` (module `github.com/Uzama/krane-event-management-platform`, Go 1.23.0); the rest of the skeleton — `git init`, `.gitignore`, `CLAUDE.md`, `FAILURES.md` — already existed, and git-ai capture was verified live rather than set up fresh.

Bundled the preceding `/init` review of the instruction files: trimmed `FAILURES.md` from 36 lines to 7 (14 of its 16 rules restated `CLAUDE.md` invariants, so both files were charging every session twice for the same rules), folding the two net-new rules into `CLAUDE.md`; pinned Go 1.23; documented the single-test command; added a pre-code note. Created `AUDIT.md` and `ISSUE.md`.

Audited `FEATURES.md` against the assignment brief and applied 8 amendments closing 6 gaps and 2 inconsistencies — CI had no owner, the diagram covered only the data model, the contributor and attendee role boundaries were unencoded and untested, DST had no test, audit recorded no actor, and item 18's dependency contradicted the file's own ordering notes. Added Phase 4 to track the four graded deliverables. Queue is now 29 items.

Commit `5e9bf10`.

## 2026-08-23 21:47 — Feature 01 · Requirement analysis + design diagram

Completed feature 01. Produced both halves of the required design diagram plus the written analysis behind them: `docs/requirements.md` (nine entities with full column tables, relationships, the per-event role matrix expressed as the `role_permissions` rows item 07 will seed, an invariant→column map, eight recorded decisions, and the remaining open question), `docs/er-diagram.md` (Mermaid `erDiagram`, constraint table, legend), and `docs/components.md` (layers, a request traced end to end through the middleware chain and back out the error path, and the compose topology). Moved the assignment brief from `doc/` to `docs/` so there is one doc directory.

Eight decisions recorded so items 02–23 don't re-litigate them. Two are scope cuts made deliberately and logged for `TRADEOFFS.md`: the invitation acceptance lifecycle (invitations stay an independent record; membership is populated directly, keeping every read path behind the per-event authz chokepoint), and cross-event physical-room conflicts (rooms are per-event, so item 16's `EXCLUDE` covers exactly the conflicts the model claims to have — the known limitation is that two events in the same building can both book Hall A). Both carry a "what two more weeks would buy" line.

The remaining open question — whether the email-visibility ban extends below attendee to contributors — has a drafted but unsent email at `docs/requirements.md` §7.3. It does not block item 02 and does not weaken the item 10 `must:` test, which asserts the attendee case the brief names.

Verification: all four Mermaid blocks parse under mermaid@11 driven by a jsdom DOM. Bare Node fails *every* flowchart with a missing-DOM error regardless of content, so the checker asserts a trivial flowchart parses first — without that self-check the run would have reported false failures. Scripted checks confirmed all nine tables item 02 names appear in both the ER diagram and the analysis, and that no invitation `status`/`responded_at` column survives anywhere.

No code, and no TDD step: this feature produces documents, and `make test` does not exist until item 03. Flagged at the review gate rather than skipped silently.


## 2026-08-23 22:40 — Feature 02 · DDL / schema migrations

Completed feature 02. One timestamped golang-migrate pair, `migrations/20260823164421_init_schema.{up,down}.sql`, carrying all nine tables from `docs/requirements.md` §2 — `users`, `events`, `event_members`, `role_permissions`, `rooms`, `sessions`, `invitations`, `audit_log`, `idempotency_keys` — with `uuidv7()` PKs, `timestamptz` throughout, `events.timezone` holding an IANA name, and session time as a single `tstzrange`.

`version` and `deleted_at` columns ship now; both partial `EXCLUDE` constraints are deferred to item 16, with their exact DDL sitting in a comment on `sessions`, so 16's race test can fail for the right reason before it passes. `role_permissions` is created empty — item 07 seeds it.

Introduced the two-role model the rest of the project depends on. `krane_migrator` owns the database and every object the migrations create; `krane_app` is the API's runtime identity. **Item 03 must set `POSTGRES_USER=krane_migrator` in compose and point the API DSN at `krane_app`** — `ALTER DEFAULT PRIVILEGES` says `FOR ROLE krane_migrator` explicitly, and if migrations ever run as a different role every future table reaches the API with no grants. `krane_app` is created with `LOGIN` and no password; item 03 sets it from the environment rather than committing a credential to a migration.

Two tables are special-cased away from the blanket DML grant, each with its own grant and its own unconditional `REVOKE` as the last statements of the file: `audit_log` is `INSERT, SELECT` only (append-only is a grant, not a convention), and `role_permissions` is `SELECT` only — the API reads the policy that governs it and cannot rewrite its own permission rules. Both revokes are order-independent by design, so a later reordering or a future blanket grant cannot silently widen them.

Three decisions were recorded in the docs rather than left implicit: **D9** — soft-deleting a session releases its room and speaker slot immediately, stated as a product rule in `docs/requirements.md` §7.3 (new) and referenced from the `deleted_at` legend line in `docs/er-diagram.md`, because item 16's partial `EXCLUDE` makes it true whether or not anyone intended it; **D10** — `event_members.role` and `invitations.role` are `CHECK`-constrained while `role_permissions.role` deliberately is not, since that table is the registry (for item 24's ADR); and the half-open `[)` range convention, documented as a column comment on `sessions.time_range` so items 12 and 16 build back-to-back sessions that do not falsely conflict.

Verification, in place of the `make test` that does not exist until item 03: fourteen gates against a throwaway `postgres:18` (18.6) container started with `POSTGRES_USER=krane_migrator`, then torn down. Two gates ran *before* any DDL was written — `uuidv7()`'s exact signature was confirmed from `pg_catalog` rather than assumed (zero-arg form; an `interval`-shifted overload also exists), and extension ordering was proven in both directions (a `citext` column without the extension errors; dropping `citext` while such a column exists errors, and `CASCADE` would take the column with it). The rest: nine tables present, zero naive timestamp columns, `tstzrange`/`citext` where the design says, v7 version nibble on generated ids, all six unique constraints, the `role_permissions` composite PK, the role `CHECK` rejecting `'organiser'` and accepting `'contributor'`, the audit and policy privilege matrices, live `permission denied` on `UPDATE`/`DELETE`/`TRUNCATE` of both protected tables while connected **as `krane_app`**, and `[)` back-to-back ranges proven not to overlap where `[]` would.

The `ON SEQUENCES` default grant was dropped after confirming the schema owns no sequences — zero sequences, zero identity columns, zero `serial` columns — but its `REVOKE` is kept in the down file as a deliberate no-op so a future sequence grant cannot strand `DROP ROLE`. That the default-privileges revokes are load-bearing was proven, not assumed: stripping them makes the down migration fail at `DROP ROLE` with a non-zero exit rather than continuing. Full `up → down → up` round trip is clean — down leaves zero tables, zero roles, zero extensions and zero default-ACL entries, and the second up restores all nine tables with the privilege matrix intact.

Gate 10 exercises FK cascade on a hard `DELETE FROM events`. It is a safety net only: events are soft-deleted, so that path does not run in normal operation and it is not evidence that deleting an event is handled — that is items 08 and 22.

No `FAILURES.md` rule added: nothing went wrong that isn't already an invariant in `CLAUDE.md`.

## 2026-08-23 23:55 — Feature 03 · docker-compose + Makefile + green pipeline

Completed feature 03. `make up && make seed && make test` — the contract the assignment is graded on — now runs green from a clean machine in **1m25s cold** (no cached images) and **14s warm**, against the five-minute budget. A fresh-clone rehearsal from the staged index, with no `.env` and no setup step, passes identically.

`docker-compose.yml` runs `postgres:18.6` and `ghcr.io/navikt/mock-oauth2-server:2.1.10` long-running, plus two one-shot services behind a `tools` profile: `migrate` (`migrate/migrate:v4.18.1`) and `app-role`. Every tag is pinned and was manifest-checked for linux/arm64 **and** linux/amd64, so the dev Mac and `ubuntu-latest` run identical images. One-shots are invoked with `docker compose run --rm` rather than `up --wait`, because `--wait` treats a service that exits as a failure — which is exactly what a one-shot does on success.

All three obligations item 02 left behind are honoured: `POSTGRES_USER=krane_migrator` (so `ALTER DEFAULT PRIVILEGES FOR ROLE krane_migrator` binds to the role that actually creates the objects), every host-facing DSN points at `krane_app`, and the `app-role` service sets `krane_app`'s password from the environment via psql's `:'app_password'` quote-and-escape form — no credential in a migration, and no string-concatenation into DDL.

**`make up` is idempotent; `make down` is the only destructive target.** This is load-bearing rather than stylistic: `make test` depends on `up`, so an `up` that wiped volumes would destroy what `seed` just wrote before `test` ever saw it — at item 14 that is 50k invitations vanishing silently mid-chain. Proven by inserting a row, re-running `up`, and reading it back. `CLAUDE.md` described `up` as a "clean start", which is the wording that would have tempted a future session to break exactly this; it was corrected in this commit, along with the now-false "repo is pre-code" note.

The one test is `migrations/schema_test.go`, and it is deliberately not a tautology. It connects to the throwaway `krane_test` **as `krane_app`** and asserts three things, each proving a different link in the pipeline: `current_user` is the runtime role (not the owning migrator, for whom `has_table_privilege` would return true for everything and make the next assertion vacuous); all nine tables exist; and the **privilege matrix** landed too — `audit_log` is SELECT+INSERT but not UPDATE/DELETE, `role_permissions` is SELECT only. That third assertion is the one that earns its keep: grants are per-database, so a migration that created every table in `krane_test` while silently skipping every GRANT and REVOKE would sail past a schema-only check, and every later authz and audit test would then be running against privileges the API does not have in production.

Each assertion was proven load-bearing by breaking its subject and watching it fail: pointing the DSN at `krane_migrator` (fails on `current_user`), dropping `idempotency_keys` (fails on the table list), hand-granting `UPDATE ON audit_log` (fails on the privilege matrix), and supplying a DSN password that does not match what `app-role` set (fails with SQLSTATE 28P01). Note that `KRANE_APP_PASSWORD=wrong make test` **passes**, and correctly so — `make up` re-applies the password from the same variable, so both ends move together; the mismatched-DSN case is what actually demonstrates enforcement.

Test-DB isolation was decided now rather than deferred, because every later package inherits it: one `krane_test` today, the standing rule that no test may assume sole occupancy of the database, and `CREATE DATABASE <pkg> TEMPLATE krane_test` per package from the first data-writing package (item 08) — a template clone is a file copy, so migrations run once for the whole suite rather than once per package. Isolation is per-package and never per-test, since item 16's race test needs two goroutines contending on the same rows. All three rules are now in `CLAUDE.md` so they cannot be re-litigated.

Dev credentials are guarded by a target, not a comment: `make up` refuses to start when `ENV=production` while `POSTGRES_PASSWORD` or `KRANE_APP_PASSWORD` still holds its committed default. Verified in both directions. `.env.example` carries a banner stating the file is committed and every value in it is public.

The Go version lives in exactly one place. `go.mod` is the source of truth; `scripts/require-go.sh` parses the `go` directive out of it rather than taking a version argument, and CI uses `setup-go` with `go-version-file: go.mod`. Nothing to keep in step by hand.

`.github/workflows/ci.yml` runs `make lint && make test` on push and PR, with `docker compose logs` on failure. **It has not executed yet** — a workflow cannot run until the branch is pushed, so it is reviewed and YAML-validated, not proven. `make lint` (gofmt + `go vet` + a pinned `golangci/golangci-lint:v1.62.2-alpine`, so local and CI run byte-identical versions) is green locally.

Two things were learned the hard way and are logged rather than quietly fixed. `ISSUE.md` carries the first: Postgres 18 refuses to start on `/var/lib/postgresql/data`, the path every older Postgres uses and every tutorial repeats — the familiar pattern was reached for and 18 rejected it, which is exactly the trap the brief warns about. The second is in `FAILURES.md`: `pgx/v5` was pinned at v5.7.6, not the current v5.10.0, because v5.8.0 onward require Go 1.24/1.25 while this repo is pinned to 1.23 — a module needing a newer Go than `go.mod` declares fails to resolve outright rather than degrading.

Deliberately out of scope, and noted so the gaps are not mistaken for oversights: no `api` service in compose (there is no `cmd/api` until item 04, which adds it; `docs/components.md` §3 already shows it in the target topology), `make seed` is a stub that names item 14, and the OIDC service has no compose healthcheck because the image ships without a shell — `make up` polls its discovery document from the host instead, and item 06 configures the issuer properly.

## 2026-08-24 01:25 — Feature 04 · Repo architecture scaffolding

Completed feature 04. `cmd/`, `internal/utils`, `internal/container`, `internal/http` (+ `handler`, `response`), and `internal/bootstrap` now exist and are wired end to end: `cmd/api/main.go` derives a `signal.NotifyContext` and calls `bootstrap.Boot(ctx)`, which loads `utils.Config` from the environment, builds a `container.Container` (pgx pool + `log/slog` JSON logger), mounts `internal/http.NewRouter` (stdlib `net/http.ServeMux` — no new dependency; Go 1.23's method+pattern routes are enough for one route), and serves `GET /health` until the context is cancelled, at which point `http.Run` shuts down gracefully. `domain/` and `adapter/` deliberately stay empty this feature — neither has a consumer yet (`domain/errors.go` lands with 07, `domain/opt` with 12/20, `adapter/postgres` with 08, `adapter/auth` with 06); creating them now would be placeholder packages with no logic and no test.

`GET /health` is a readiness check, not mere liveness: it pings the db pool through a local `Pinger` interface (`type Pinger interface { Ping(ctx context.Context) error }`) that `*pgxpool.Pool` satisfies structurally, so `internal/http` never imports `pgx` even transitively — the template every later handler should copy (depend on the narrowest interface, not a concrete adapter type). Failure returns 503 through the one error envelope from `CLAUDE.md` (`internal/http/response`, introduced here rather than as a later stub), with `details` always rendering as `{}`, never omitted.

`internal/utils/db.go`'s `NewPool` fails fast and is bounded twice over: `pgxpool.ParseConfig` + `ConnConfig.ConnectTimeout = 5s` bounds the dial, and the pool's own startup `Ping` runs under a second `5s` context. Both are proven, not assumed: the fail-fast tests (`utils`, `container`, `bootstrap`) point at a local `net.Listen` that never calls `Accept` — the kernel completes the TCP handshake into the backlog (genuinely routable/connectable) but nothing ever speaks the Postgres protocol back, reproducing "routable but dead" deterministically on loopback rather than depending on how the sandbox's network handles an unroutable IP. All three assert the failure returns in well under the test's own bound, not never.

Graceful shutdown lives in `internal/http/server.go`'s `Run(ctx, srv, ln, logger)`, decoupled from `bootstrap` so it's unit-testable on its own terms: `TestRun_GracefulShutdown` binds an ephemeral port, dials it to prove the server is really serving, cancels the context, and asserts both that `Run` returns and that the port stops accepting connections afterward — shutdown proven as a behavior, not just "returns nil." `NewServer` sets `ReadHeaderTimeout`/`ReadTimeout`/`WriteTimeout`/`IdleTimeout` rather than leaving `http.Server` at its zero-value (unbounded) defaults.

`docker-compose.yml` gains the `api` service item 03 flagged as owed, built from a new root `Dockerfile` (multi-stage, pinned `golang:1.23.12-alpine3.22` → `alpine:3.22.5`, exact patches confirmed to exist on Docker Hub before pinning — same discipline as the rest of the stack's images). It is **not** part of `make up`'s default set: `make test` runs `go test ./...` directly against Postgres on the host, the same pattern `migrations/schema_test.go` already established, so the graded pipeline never needs a containerized API process. The service exists for `docker compose up api` / a full local-stack run, matching `docs/components.md`'s topology — verified manually (`docker compose build api && docker compose up api`, `curl /health` → 200, real container-to-container DB connectivity). `.env.example` and the `Makefile` gained `HTTP_PORT`, `LOG_LEVEL`, `API_PORT` following the existing `POSTGRES_PORT`/`OIDC_PORT` naming pattern. `go.mod` picked up `pgxpool`'s own transitive deps (`puddle/v2`, `x/sync`) as indirect requires — no new top-level dependency, and `go.sum` had no diff (the hashes were already present under `pgx/v5`).

Verification: `make test -race` green across all seven packages (`bootstrap`, `container`, `http`, `http/handler`, `http/response`, `utils`, `migrations`); `make lint` green (gofmt/govet/golangci-lint, including errcheck/bodyclose/rowserrcheck — no ignored errors anywhere in the new code). Manual sanity beyond the automated suite: `go run ./cmd/api` against the real dev stack → `curl /health` → 200; Postgres stopped mid-run → 503 with the standard envelope; `SIGINT` → `"shutting down"` log line → clean exit, no hang.

One environment incident, not a code defect: mid-verification, the host Mac's disk filled to 100% capacity, which took Docker Desktop's VM storage down with `input/output error`s across every container on the machine, not just this project's — `make down`'s own cleanup failed the same way. Freeing disk space and restarting Docker Desktop resolved it; re-running `make test -race` and `make lint` afterward confirmed nothing in the new code was implicated. No `FAILURES.md` line added for this — it's a host resource issue, not a repeatable code mistake.

Carried forward, not done here: before item 05, verify oapi-codegen's generator actually targets `net/http.ServeMux` cleanly, or reopen the router choice then rather than carry a mismatch forward — item 04 only established that stdlib is sufficient for a health-only surface.

## 2026-08-24 07:17 — Feature 05 · OpenAPI harness

Completed feature 05. `openapi/openapi.yaml` (OpenAPI 3.0.3) is now the documented contract, starting with `GET /health` — `HealthStatus{status,database}` on 200, the shared `ErrorEnvelope` (matching `internal/http/response/error.go` field-for-field) on 503. `openapi/spec.go` embeds it (`//go:embed`) so nothing downstream depends on a filesystem path. From here, every endpoint updates the spec first.

Item 04's carried-forward question — whether oapi-codegen actually targets stdlib `net/http.ServeMux` — is answered: it does, cleanly. `internal/http/gen` (generated via `internal/tools/tools.go`'s pinned `oapi-codegen/v2 v2.6.0`, driven by `//go:generate` in `internal/http/gen/generate.go`) uses the `std-http-server` target, producing a `ServerInterface` and `HandlerFromMux`/`HandlerWithOptions` that default to `http.NewServeMux()` — confirmed by actually running the generator and inspecting its output, not by reading docs. `internal/http/router.go` is unchanged in behavior; its comment now records the choice as confirmed rather than open.

`internal/http/validator` is the `CLAUDE.md`-named validation package: `LoadSpec`/`ValidateRequest`/`ValidateResponse` wrap `kin-openapi`'s `openapi3filter` and `routers/legacy` (the router-agnostic path matcher — chosen specifically to avoid a routing-framework dependency, since this package only ever runs inside tests). Per FEATURES.md's literal wording ("wired into tests"), validation is **not** live production middleware: `router.go`'s production request path is untouched. `internal/http/router_test.go` boots the real, unmodified router and validates the **live recorded** 200 and 503 responses against the spec, so spec/handler drift is caught in either direction — this, plus `internal/http/validator/validator_test.go`'s negative-path proofs (`POST /health` has no matching route; a response missing a required field is rejected), is what "CI verifies the server actually honours the spec" means here.

CI's other half is drift detection: `make generate` regenerates `internal/http/gen` from the spec; `make contract-check` runs it and then `git diff --exit-code -- internal/http/gen openapi` — proven, not assumed, by deliberately editing the spec without regenerating (fails, diff shown) and reverting (passes again). `.github/workflows/ci.yml` runs it as a new `Contract check` step, after `Lint` and before `Test`, so drift fails fast before the Docker/Postgres stack spins up.

Two dependencies were added — `github.com/oapi-codegen/oapi-codegen/v2 v2.6.0` (tool only, via `internal/tools`) and `github.com/getkin/kin-openapi v0.133.0` — chosen after an explicit decision **not** to bump the Go toolchain: an earlier draft of this feature proposed moving `go.mod` to Go 1.25 to reach the newest oapi-codegen/kin-openapi releases (which require 1.24/1.25), but that was rejected — a dependency's own `go` directive is a floor for the toolchain, not a mandate to raise this module's declared version, and nothing in this API needs OpenAPI 3.1. `go.mod`, `pgx`, `golangci-lint`, and the Docker base images are all untouched.

That rejection paid for itself during verification: pinning `kin-openapi` to v0.135.0 (the newest release whose own `go` directive still resolves against 1.23) compiles fine in isolation but **breaks oapi-codegen v2.6.0's own source** (`pkg/codegen/schema.go` assumes an older `openapi3.SchemaRef.Ref` field shape) once both land in the same module graph — caught by actually running `go build`/`go vet`/the codegen tool in a scratch module, not by checking `go` directives alone. Letting `go mod tidy` resolve the minimum compatible version naturally lands on v0.133.0, which builds, vets, and generates real output cleanly. The same round of verification cut a dependency that had been planned: `oapi-codegen/nethttp-middleware` only wraps *requests* as production middleware (which this feature doesn't use) and can't validate responses either way, so `internal/http/validator` calls `kin-openapi`'s `routers/legacy` + `openapi3filter` directly instead — one fewer dependency than originally planned.

`make lint` (golangci-lint v1.62.2, unchanged) passes on all new code including the generated `health.gen.go`, with no new `.golangci.yml` exclusion — its default generated-file detection (the `Code generated ... DO NOT EDIT` header) was enough. `go vet` shows no deprecation warning on the `routers/legacy` import. `make test` is green across all packages including the two new ones.

No `FAILURES.md` line added: the two things that went sideways during planning — the Go-1.25 ripple effect, and the kin-openapi/oapi-codegen version mismatch — were both caught before any code shipped, not after a runtime failure.

## 2026-08-24 08:35 — Feature 06 · Auth (validate-only)

Completed feature 06. `internal/domain/` and `internal/adapter/` exist for the first time. `internal/domain/user` is the aggregate: `entity.go` (`User`, `Claims` — the latter deliberately living in domain, not `adapter/auth`, so `http/middleware` can depend on the same type both `adapter/auth` produces and `domain/user.Service` consumes without ever importing `adapter` directly), `port.go` (`Repository`), `service.go` (`GetOrCreateBySubject`, a thin pass-through — the atomicity guarantee belongs to Postgres, not application code), and `errors.go` (`ErrTokenInvalid`, `ErrMissingClaims` — two new sentinels, not the four `ErrConflict`/`ErrVersionMismatch`/`ErrForbidden`/`ErrNotFound` named in `CLAUDE.md`'s target layout, which stay deferred to item 07, their first real consumer).

`internal/adapter/postgres/user_repo.go` implements the upsert as `INSERT ... ON CONFLICT (subject) DO NOTHING RETURNING ...`, falling back to a plain `SELECT` only when the insert affected nothing — the `INSERT` is the one atomic decision point; the fallback `SELECT` never decides anything, so there's no check-then-act window. **Decision, not an oversight:** `email`/`name` are captured once, at row creation, and never refreshed on a later sign-in — proven by a test that signs in twice with a *different* email/name the second time and asserts the row is untouched. Re-syncing profile fields on every login would reopen `users.email`'s unique-constraint conflict case (two subjects claiming the same new email) for no benefit a validate-only feature needs; revisiting this is a fair scope item for whichever later feature actually needs live profile sync.

`internal/adapter/auth/verifier.go` wraps `github.com/coreos/go-oidc/v3` (pinned `v3.13.0` — the newest version whose own `go` directive still resolves at our `go 1.23.0`; the latest, v3.20.0, requires `go 1.25.0`). Audience checking is enforced explicitly (`oidc.Config{ClientID: audience}`, never `SkipClientIDCheck`) — the plan review flagged this as the single most likely "your auth has a hole" gap, and it's covered by a dedicated wrong-audience test, not just implied by the code. Every verification failure returns one of `user.ErrTokenInvalid` (bad signature, expired, wrong audience, wrong issuer — "someone sent garbage") or `user.ErrMissingClaims` (verifies cleanly but lacks `sub`/`email`/`name` — "the issuer isn't sending what we require"), a distinction `http/middleware/auth.go` surfaces as a structured `reason` log field (`token_invalid` vs `missing_claims`) while still returning 401 either way — the HTTP contract doesn't change, but item 08's on-call debugging can tell a forged token from an issuer misconfiguration at a glance.

`internal/http/middleware/auth.go` follows the `Pinger` pattern from item 04: two narrow local interfaces (`TokenVerifier`, `UserResolver`), never a concrete adapter type, so `http` still never imports `adapter`. `Auth()` attaches the resolved `user.User` to the request context (`UserFromContext`); every rejection path — missing header, malformed header, verifier error, resolver error — returns 401 through the standard envelope, proven never to panic into a 500 by dedicated tests for each path.

Two dependencies added: `github.com/coreos/go-oidc/v3 v3.13.0` and its own direct dependency `github.com/go-jose/go-jose/v4 v4.0.5` (used directly in test code to mint/sign tokens against a local fake JWKS double — the same JOSE library go-oidc verifies against, not a second crypto library). Both, plus their transitive `golang.org/x/oauth2`/`x/net`/`x/crypto`, resolve cleanly at `go 1.23.0` with no toolchain bump (`GOTOOLCHAIN=local`, confirmed via `go build`/`go vet` in isolation before writing any consuming code — same discipline item 05 recorded). No change to the Go toolchain, `pgx`, `golangci-lint`, or the Docker base images.

`docker-compose.yml`'s `oidc` service now carries a `JSON_CONFIG` (empirically verified against the exact pinned `mock-oauth2-server:2.1.10` image, not just its docs, by running it standalone first) with four claim mappings: three fixed demo identities (`demo-admin`/`demo-contributor`/`demo-attendee`, matched on `client_id`) for `make token`, and one test-only `test_sub`/`test_aud` templated mapping so Go integration tests can mint a token for an arbitrary, unique subject — and deliberately wrong audiences — without touching the three fixed identities. **Caught before it shipped:** Docker Compose interpolates `${VAR}` in every compose-file string value, including this multi-line one, before the container ever sees it — the first version of this config silently defaulted `test_sub`/`test_aud` to blank strings, discovered only by minting a token and decoding it. Fixed by escaping as `$${test_sub}`/`$${test_aud}` so the literal `${...}` reaches mock-oauth2-server's own template engine instead. Logged as a `FAILURES.md` rule.

`internal/container/container.go` grows two fields, `AuthVerifier` and `Users`, both built in `New()` — OIDC discovery now happens at boot (bounded by its own internal 5s timeout, mirroring `NewPool`'s ping, since `bootstrap.Boot`'s signal-context has no deadline of its own), so a misconfigured `OIDCIssuerURL` fails boot, not the first request. This is the literal "wire the mock OIDC issuer" instruction from `FEATURES.md`. `internal/utils/config.go` gains `OIDCIssuerURL` (default `http://localhost:9090/default`) and `OIDCAudience` (default `krane-api`); the `Makefile` derives `OIDC_ISSUER_URL` next to the existing `OIDC_DISCOVERY` and defaults `OIDC_AUDIENCE ?= krane-api`, both falling under the existing bare `export` so `go test` sees them exactly like `DATABASE_URL` already does. `docker-compose.yml`'s `api` service gets both set explicitly to the container-network address (`http://oidc:8080/default`), mirroring how its `DATABASE_URL` already differs from the host-facing Makefile one.

**No business route consumes the middleware yet — deliberate, not an oversight.** `router.go`, `bootstrap.go`'s route wiring, and `openapi/openapi.yaml` are all untouched; item 08 (Events CRUD) is the first real consumer. Proof of correctness is `internal/http/middleware/auth_integration_test.go`, which builds the real `Verifier` (real JWKS discovery against the running mock issuer) and the real `user.Service` (real `krane_test`), mounts a throwaway protected handler that exists only in the test file, and covers every case the plan review demanded: no token, valid token (asserting the *correct* mapped user reached the handler, not just a bare 200), malformed token (401, not 500), wrong audience, wrong issuer (via `POST /other/token` — mock-oauth2-server's multi-issuer routing needs no extra config, confirmed empirically before writing the test), and the same fresh subject authenticating twice (same user id both times, exactly one row in `users` — the upsert proven end to end, not just at the repository-unit level). Every integration test mints its own unique subject so parallel packages can't collide on `users.subject`'s unique constraint.

`make token USER=admin|contributor|attendee` shells out to the mock issuer's own token endpoint — no Go code, no API dependency, matching "never issue/sign tokens in the API." The extraction (`grep`/`sed`, no new tool dependency) is whitespace-tolerant, which matters: mock-oauth2-server 2.1.10's JSON responses have spaces around colons (`"access_token" : "..."`), so the obvious `grep -o '"access_token":"[^"]*"'` silently finds nothing — caught by decoding an actual minted token, not by reading the response shape. The Makefile comment states plainly that `USER` names a demo identity for convenience, not a privilege — tokens carry `sub`/`email`/`name` only; roles are per-event and live entirely in `event_members`, starting item 09.

Verification: `go build`/`go vet`/`go mod tidy` clean with no toolchain change; `make up` idempotent with the new `JSON_CONFIG` accepted; `make token USER=admin` decoded to the exact expected claims; `make test` green across all packages (new: `adapter/auth` 8 tests, `adapter/postgres` 2, `domain/user` 2, `http/middleware` 11 across unit + integration); `make lint` green after fixing three real `golangci-lint` findings (two `errorlint` non-wrapping `%v` format verbs in `verifier.go`, one `errors.Is` vs `!=` on `pgx.ErrNoRows` in `user_repo.go`, three unchecked `resp.Body.Close()` in test helpers); `make contract-check` shows no drift, confirming the OpenAPI surface is genuinely untouched.

One `FAILURES.md` rule added: the Docker Compose `${VAR}` interpolation trap above.

## 2026-08-24 13:10 — Feature 07 · Authorization chokepoint

Completed feature 07. `internal/domain/authz` and `internal/adapter/authz` exist for the first time, plus `migrations/20260824090000_seed_role_permissions.{up,down}.sql`. The permission matrix itself was not a new decision — `docs/requirements.md` §4 (written in item 01, referenced by the item 02 migration's own header comment) already specified every `(resource, action, role)` cell from the brief; this feature seeds it and enforces it, nothing more. Seeded exactly 31 rows (admin 17, contributor 12, attendee 2), confirmed against `SELECT count(*) FROM role_permissions` and by a dedicated exact-set test (`TestRolePermissionsSeed_RowCountMatchesFullyExpandedMatrix`) comparing the seeded rows against an independently-transcribed copy of the doc's table, not against the migration file itself — so a dropped or mis-typed `INSERT` fails a test instead of silently denying a permission that should exist.

`internal/domain/authz/port.go` declares `Action`/`Resource` string-const types and the one-method `Policy` interface (`Can(ctx, userID, eventID, action, resource) (bool, error)`) — per `docs/components.md`'s layer table, the interface belongs in `domain`, unlike item 06's `TokenVerifier`/`UserResolver`, which are local seams in `http/middleware` because there's no domain-level policy for auth the way there explicitly is for authz.

`internal/adapter/authz/policy.go` implements it with two deliberately different caching strategies, documented as such in code so item 24's ADR doesn't blur them: `role_permissions` is loaded once at boot into an in-memory map and never refreshed (it is read-only to the API at the DB grant level from item 02, and changes only via a migration plus redeploy — not dynamic, runtime-editable authorization); `event_members` is queried live on every `Can` call and never cached, which is what will make "an admin removes a member and their next request is denied" true without any invalidation code once item 09 adds that path. `New` also fails boot if any of the three roles `event_members`/`invitations` can carry (`admin`/`contributor`/`attendee`) has zero permission rows — a role with no rows answers every question `(false, nil)`, indistinguishable from a real deny, so an incomplete seed now fails loudly at startup instead of surfacing later as silent fail-closed. That check (`validateRoleCoverage`) is exercised as a pure function against a synthetic map in a white-box `policy_internal_test.go`, not by deleting real rows from the shared `role_permissions` table — mutating that table mid-test would race every other package hitting the same `krane_test` instance across a parallel `go test ./...` run (CLAUDE.md's isolation rule), a mutation-based version of the test was planned and rejected for exactly this reason before it was written.

**Deliberate, documented non-leak:** a non-existent event id and "real event, not a member" produce the identical `(false, nil)` from `Can`, surfaced as `403`, never `404` — a caller with no standing on an event has no standing to learn whether it exists. Proved explicitly (not left as an accidental pass) at both the policy layer and the full HTTP stack, and now recorded as point 4 in `docs/requirements.md` §4.

`internal/http/middleware/authz.go` is the enforcement half: mounted per route like `Auth`, parameterized by the resource/action that route performs, reading the actor from context and the event id from `r.PathValue("eventId")` (every authz-protected route is `/v1/events/{eventId}/...` — confirmed by the matrix having no `event:create` row, since creating an event precedes any `event_members` row to check against). Denied → `403 forbidden` with a generic message; a policy error or a missing actor/eventId (a wiring bug, since `Authz` must run after `Auth`) → `500`, logged, never conflated with a real `403`. `auth.go` gained one exported test seam, `ContextWithUser`, carrying an explicit "NOT FOR PRODUCTION USE — an authentication bypass if a handler ever called it" comment, since production user identity must come from exactly one place: a validated token, mapped by `Auth`.

**No business route consumes authz yet — same shape as item 06, not an oversight.** `router.go` and `openapi/openapi.yaml` are untouched; items 08/09 are the first real consumers. Item 07's own `must:` — a contributor gets 403 attempting a role change — is proved by `internal/http/middleware/authz_integration_test.go`, chaining the real `Auth` → real `Authz` → a throwaway handler that writes a distinctive body, against the real mock OIDC issuer and real `krane_test` Postgres. Every `200` case asserts that exact body, not just the status code, so it proves the request actually traversed the full chain rather than some earlier branch happening to also return 200.

`internal/container/container.go` gains `Authz *authz.Policy`, wired in `New()` with the same fail-fast-at-boot treatment as the OIDC verifier; `container_test.go`'s `TestNew_Succeeds` asserts it's non-nil, same as `AuthVerifier`/`Users`.

Verification: `make test -race` green across all packages, including the three new test files (`policy_internal_test.go`, `policy_test.go`, `authz_test.go`, `authz_integration_test.go`); `make lint` clean; `make contract-check` shows no drift (openapi/router untouched, as expected). No `FAILURES.md` rule added — nothing here went sideways at runtime; the one process correction (rejecting the mutation-based boot-check test in favor of the white-box pure-function version) was caught during implementation review, before any code shipped.

## 2026-08-24 16:50 — Feature 08 · Events CRUD

Completed feature 08 -- the first real business route, and the first feature to establish several cross-cutting patterns every later CRUD resource (rooms, sessions, invitations) will copy: `domain/opt.Optional[T]`, `domain/errors.go`'s four sentinels, an audit-log writer, and keyset pagination. Four scope questions were genuinely open going in -- the permission matrix has no `event:create` row, and `Optional[T]`/audit/pagination don't exist in code yet even though their owning features (09/10/17/19/20/22) all declare `needs: 08` or later -- resolved via `AskUserQuestion` before any code was written: **(D1)** update ships as `PATCH` now, pulling `Optional[T]` forward rather than shipping `PUT` and breaking the contract at item 20; **(D2)** `GET /v1/events` scopes by an `event_members` SQL join (never a post-fetch `Can()` loop) and paginates with a real keyset cursor now, not deferred to item 19; **(D3)** every mutation writes `audit_log` in the same transaction now, with item 22 owning the full dedicated suite; **(D4)** `POST /v1/events` atomically grants the creator an admin `event_members` row plus **two** audit rows (`entity_type` `event` and `event_member`), since a membership insert is a privilege grant and item 09's explicit add-member must reuse the identical shape.

`internal/domain/errors.go` (new, package `domain`, sibling to the aggregate subpackages) declares `ErrConflict`/`ErrVersionMismatch`/`ErrForbidden`/`ErrNotFound` -- this feature uses `ErrVersionMismatch` (409) and `ErrNotFound` (404); `ErrConflict`/`ErrForbidden` are declared now per `CLAUDE.md`'s fixed four-error set but have no caller yet. `internal/domain/opt/optional.go` is `Optional[T]{Value T; Set bool}` with a hand-written `UnmarshalJSON` -- `Set` only ever becomes true when `encoding/json` actually calls it, which only happens when the key was present at all, so absent/null/value are three distinct outcomes a plain `*T` cannot produce. `internal/domain/event` (`entity.go`/`port.go`/`service.go`) is a thin pass-through service over a `Repository` port, matching `domain/user.Service`'s precedent: every invariant lives at the `http/request` boundary (validation) or in the repository (atomicity), not here.

`internal/adapter/postgres/event_repo.go` is where D1-D4 actually get enforced. `Create` is one `pgx.Tx`: a single CTE statement inserts `events` then `event_members(role='admin')` in the same round trip (`INSERT ... RETURNING ... ), new_member AS (INSERT ... SELECT id, $actor, 'admin' FROM new_event RETURNING ...)`), followed by two `audit_log` inserts (`entity_type='event'` and `entity_type='event_member'`, the latter's `entity_id` set to the **membership row's own PK**, never the event's id -- proved by a dedicated assertion since this is exactly the shape item 09's explicit add-member must reuse) -- all four writes commit or roll back together, proved by deliberately violating `events_ends_after_starts_check` inside `Create` (bypassing the `http/request` 422 guard on purpose) and asserting zero rows in `events`, `event_members`, and `audit_log` afterward. `Update`/`Delete` share one `versionedWrite` helper: a single goqu-built `UPDATE ... WHERE id=$ AND version=$ RETURNING to_jsonb(OLD) AS before_row, to_jsonb(NEW) AS after_row` -- PG18's `RETURNING OLD`/`NEW` correlation names, confirmed against real Postgres before writing any Go (a `psql` scratch session first, since `goqu.I("NEW.id")` quotes the identifier as `"NEW"."id"` and Postgres then reports "missing FROM-clause entry for table NEW" -- the correlation name must stay a raw, unquoted `goqu.L(...)` literal). Zero rows affected triggers one diagnostic `SELECT` **after** the failed write (never before) to decide `ErrNotFound` (soft-deleted or missing) vs `ErrVersionMismatch` (409) -- this is not check-then-act since nothing about the failed write depended on it. `List` joins `event_members` and paginates over `(created_at, id)` via PostgreSQL row-value comparison (`(e.created_at, e.id) > ($2, $3)`), backed by a new migration (`events_keyset_idx ON events (created_at, id) WHERE deleted_at IS NULL`, mirroring `invitations_keyset_idx`'s shape) -- proved against a genuine tie: the test forces several rows onto one identical `created_at` via a direct `UPDATE`, since distinct timestamps would let a broken created_at-only implementation pass anyway (confirmed by deliberately breaking both the `ORDER BY` and the cursor `WHERE` down to `created_at` alone and watching page 2 come back empty, then restoring and confirming green again).

`internal/http/request/event.go` validates at the boundary (`CLAUDE.md`: "only validate at system boundaries") before the service ever sees a `CreateEventRequest`/`PatchEventRequest`: `ends_at > starts_at` for create, and for patch, `starts_at`/`ends_at` must be **both or neither** -- a deliberate design choice that keeps the invariant checkable purely from the request body, with no read-before-write ever needed to validate a partial time-window change against a stored value. `internal/http/response/event.go` is one flat `EventResponse` for every role (the event resource itself has no roster/email to hide -- item 10's presenters are about the member resource) plus `EventListResponse{data, next_cursor}`, `next_cursor` encoded via the new `internal/utils/cursor.go` (opaque base64 over `(created_at, id)`, malformed/tampered input always errors, never panics). `internal/http/handler/event.go` is thin: decode, `Validate()`, call the service, map `domain.ErrNotFound`→404 and `domain.ErrVersionMismatch`→409, encode. `DELETE` takes `version` as a **required query parameter** (no ETag/If-Match middleware exists yet -- item 17's likely home for one) -- every malformed shape (absent, empty, zero, negative, non-numeric) is rejected with 422 **before** the service is ever called, proved by a fake service that records whether it was invoked (`TestDeleteEvent_MissingOrInvalidVersion_Returns422WithoutCallingService`), so lost-update protection genuinely covers delete, not just update.

`internal/http/router.go` grew a `RouterDeps` struct (positional params stopped scaling past `/health`): `POST /v1/events` and `GET /v1/events` run behind `Auth` only, never `Authz` -- the permission matrix has no `event:create` row (item 07 already noted this), and list-scoping is a SQL join against `event_members`, not a per-resource `Can()` loop (`docs/requirements.md` §4 point 3: access is membership, and item 10's later per-role field-visibility work is a separate concern from this row-level scoping). The three `{eventId}` routes run `Auth` then `Authz`, identical shape to every other authz-protected route. `openapi/openapi.yaml` gained `Event`/`EventCreateRequest`/`EventPatchRequest`/`EventList` schemas, the 5 paths, and a `bearerAuth` security scheme -- the first time the spec documents anything beyond `/health`. `internal/http/gen`'s output file is renamed `health.gen.go` → `api.gen.go` in `cfg.yaml` (it stopped being health-only); adding `security:` blocks meant `openapi3filter.ValidateRequest` started failing every call with "missing AuthenticationFunc" until `internal/http/validator/validator.go` gained `Options.AuthenticationFunc: openapi3filter.NoopAuthenticationFunc` -- confirmed safe by reading kin-openapi's own source (`NoopAuthenticationFunc` is a literal `return nil`, inspects nothing) and by `TestEventsIntegration_NoToken_Returns401`, which hits the real production router with no bearer token at all and gets a real 401 from the real `Auth` middleware -- proof this package (test-only, never in the production request path, per its own doc comment) removed zero security coverage.

Two dependencies added, both version-checked against `go 1.23.0` before pinning per `FAILURES.md`'s rule: `github.com/doug-martin/goqu/v9 v9.19.0` (its own `go.mod` says `go 1.12`) for the PATCH's dynamic `SET` clause -- already named in `CLAUDE.md`'s fixed stack, just unused until this feature's first repository needed query building; and `github.com/oapi-codegen/runtime v1.2.0`, needed once the spec grew query parameters -- versions v1.3.0 through the latest v1.7.0 all declare `go 1.24`, so `go get`'s first attempt at the newest release correctly refused to resolve, and v1.2.0 (`go 1.20`) was confirmed to both build and pass `go vet` against the generated `api.gen.go` before being kept.

`internal/http/events_integration_test.go` is the first test to exercise the real production router (`apihttp.NewRouter`) wired to the real mock OIDC issuer and real `krane_test` Postgres all at once -- items 06/07 proved `Auth`/`Authz` only against throwaway handlers. Covers: the full create→get→list→patch→delete→get(404) happy path with per-step OpenAPI contract validation; a contributor 403 on patch/delete (matrix: admin-only) while still reading fine; a manually-fixtured attendee membership, same shape; a non-member 403 on a real, live event (item 07's non-leak rule, reproved at the full route); a non-member 403 on a **soft-deleted** event specifically (added on review -- proves the 403/404 split isn't an existence oracle in either direction: only a member gets 404-for-deleted, a non-member gets the identical 403 a non-existent event would give); a malformed cursor 400; and a bare `TestEventsIntegration_NoToken_Returns401` against the real router (also added on review, to make the `NoopAuthenticationFunc` boundary claim demonstrable rather than merely stated).

Verification: `go test ./... -race -count=1` green across every package (60+ new tests); `make lint` clean; `make contract-check` shows no drift after `git add`-ing the spec/generated files (a plain `make contract-check` against an uncommitted-but-unstaged spec correctly fails -- confirmed this is `git diff`'s normal unstaged-vs-index behavior, not real drift, by staging and re-running clean). `make test`'s full `up`→recreate-`krane_test`→migrate→test chain passes end to end. One `FAILURES.md` rule added below.

## 2026-08-24 — Feature 09 · Event membership + roles

Completed feature 09 -- add/remove members and assign per-event roles, making the item 07 chokepoint's `member` resource actually reachable and providing the roster item 10 will shape per role. Three design gaps were resolved via `AskUserQuestion` before any code was written, then two of the three mechanisms proposed for them were caught and corrected at the plan-review gate before implementation started: **(D1)** add-member identifies the target by email, resolved atomically inside the same `INSERT` (no separate lookup query) -- but the naive "atomic INSERT...SELECT is enough" framing missed that a non-matching email is a *silent no-op insert*, not an error, so `Create` explicitly checks rows-affected and disambiguates via a diagnostic read after the fact; **(D2)** last-admin protection (block removing/demoting an event's only remaining admin) is enforced by `SELECT ... FROM events WHERE id=$1 FOR UPDATE`, not the originally-proposed bare `WHERE ... AND EXISTS(another admin)` guard, which is a genuine TOCTOU race under READ COMMITTED -- proven, not just asserted, by forcing the race open with the lock removed (zero admins resulted) and closed with it present (exactly one writer wins); **(D3)** `event_members` gained a `version` column via a new migration, since item 02 shipped the table without one -- a real gap against CLAUDE.md's blanket "every mutable row has a version column" invariant, not a pre-existing decision.

**Gate-0, before any Go was written:** `RETURNING to_jsonb(OLD)`/`to_jsonb(NEW)` on both `UPDATE` and `DELETE`, the Create CTE's atomic email-lookup-plus-privilege-guard, and the `FOR UPDATE`-locked last-admin guard were all verified against a scratch Postgres 18.6 container first (matching item 02's `uuidv7()` precedent) -- confirmed the syntax parses and the guard logic behaves exactly as designed before the equivalent Go was committed to.

`internal/domain/member` (`entity.go`/`port.go`/`service.go`) is `domain/event`'s exact shape: a thin pass-through `Service` over a `Repository` port, `Member{..., UserEmail, UserName}` carrying the `users` join fields a roster needs (item 10 owns hiding them per role, not this feature -- one flat shape, matching how item 08 shipped one flat `EventResponse` before item 10 existed).

`internal/adapter/postgres/member_repo.go` is where D1-D3 are enforced, entirely inside single-transaction, single-or-two-statement writes -- no check-then-act anywhere:
- `Create` is one `INSERT ... SELECT ... FROM users WHERE email=$1 AND ($3='attendee' OR EXISTS(actor is admin))` -- the same statement both resolves the email *and* enforces that a contributor (who has `member:create` but not `member:assign-role`) cannot grant an elevated role through the back door, since granting any role is itself an assign-role-shaped action. Zero rows, no error, is disambiguated by a diagnostic read *after* the failed insert into `ErrNotFound` (no such user) vs `ErrForbidden` (actor can't grant that role); a genuine unique-constraint violation (`23505` on `event_members_event_id_user_id_key`) maps to `ErrConflict` → 409 `already_member`. Reuses item 08's exact audit shape -- `entity_type='event_member'`, `entity_id=<the new row's own id>`, `action='create'` -- proved by a dedicated assertion, per item 08's own forward note.
- `AssignRole`/`Delete` both open with `SELECT id FROM events WHERE id=$1 FOR UPDATE`, serializing every write on that event that could change its admin count, then a single version-gated, last-admin-guarded `UPDATE`/`DELETE` (`role<>'admin' OR EXISTS(other admin)`) with `RETURNING to_jsonb(OLD)`/`to_jsonb(NEW)` for the audit row. Zero rows affected triggers one diagnostic read (still inside the lock) that decides `ErrNotFound` / `ErrVersionMismatch` (409 `version_conflict`) / `ErrConflict` (409 `last_admin` -- a state conflict, not a permission failure, so 409 not 403) -- the same disambiguation pattern item 08's `versionedWrite` established, extended by one more case. `Delete` is a real hard `DELETE`: `event_members` has no `deleted_at` column, unlike events/sessions.
- **The locking discipline is a convention, not a constraint the database enforces** -- nothing stops a future write path on `event_members` from skipping the `FOR UPDATE` lock and silently reopening the orphan race. Documented in `FAILURES.md`, and recorded as a two-more-weeks item in `docs/requirements.md` §8 (a deferred constraint trigger asserting `count(admin)>=1` per event would be violation-proof regardless of whether a future call site remembers the convention) pending `TRADEOFFS.md`'s creation at item 26 -- §8 now carries a note that item 26 must migrate every entry there, not just the newest.

**The race test itself needed its own fix, and it is the concrete AI-mistake case for item 25** (full account in `ISSUE.md`, 2026-08-24): the first version -- two goroutines on a bare `sync.WaitGroup` barrier, each removing one of an event's two admins -- passed cleanly, but proved nothing. Deliberately removing the `FOR UPDATE` lock and rerunning it 20 times with `-race` never once caught the regression: a single fast indexed `DELETE`'s vulnerable window is narrower than ordinary goroutine/network scheduling jitter, so the two deletes almost always landed sequentially by accident. Forcing the window open with a delay embedded *inside* the guard's own `WHERE` clause (two earlier attempts -- a leading `pg_sleep` statement, then `EXISTS(SELECT pg_sleep(...))` -- both silently failed to delay anything, for two different reasons, see `ISSUE.md`) reproduced the real bug: both deletes succeeded, event orphaned. The fix was a deterministic mechanism, not a longer sleep: `AfterEventLockForTest`, an exported test-only synchronization seam (same "NOT FOR PRODUCTION USE" pattern as `middleware.ContextWithUser`) called immediately after the lock is acquired, gated through an internal `afterEventLock()` that checks `testing.Testing()` so a production misfire can never fire the hook while holding a live row lock. Both `AssignRole` and `Delete` -- two separate lock call sites -- now have their own deterministic proof (`member_repo_race_test.go`'s `*_EventLockSerializesAdminCountAffectingWrites` tests), each verified in both directions before being trusted: green with the lock present (12/12 across repeated runs), red with it deliberately removed (6/6, both paths), lock restored, full suite reconfirmed green.

`internal/http/request/member.go` (`AddMemberRequest`, `AssignRoleRequest` -- no `Optional[T]` needed, role is always the field being set) and `internal/http/response/member.go` (`MemberResponse`, `MemberListResponse` with the same opaque cursor as events) follow the event DTOs' shape exactly. `internal/http/handler/member.go` maps the same `ErrConflict` sentinel to two different JSON `code`s depending on which operation produced it (`already_member` on create, `last_admin` on assign-role/delete) -- each handler method owns its own error switch rather than sharing one, since the sentinel's meaning is operation-specific. `internal/http/router.go`/`container.go`/`bootstrap.go` wire `Member`/`Members` through exactly like `Event`/`Events`; the four routes are `Auth` then `Authz(member, create|read|assign-role|delete)`, one route per `role_permissions` action. `openapi/openapi.yaml` gained the two new paths and four new schemas; `internal/http/members_integration_test.go` proves the full stack end to end against the real router, real mock OIDC issuer, and real Postgres -- including reproving item 07's own `must:` (a contributor gets 403 attempting a role change) at a real route for the first time, rather than only against item 07's throwaway handler.

Verification: `go test ./... -race -count=1` green across every package; `make lint` clean; `make contract-check` clean after staging the spec/generated files; `make up && make test` full chain green end to end. Two `FAILURES.md` rules added (the locking-protocol convention; a bare goroutine barrier is not sufficient proof for a fast single-statement Postgres race, verify by forcing the window open in both directions before trusting the test).
